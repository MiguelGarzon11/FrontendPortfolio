services:
  portfolio-dev:
    build: 
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "4200:4200"
      - "4000:4000"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true